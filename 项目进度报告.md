
# GeneTransformer 项目进度报告

**更新时间**: 2026-09-13
**整体进度**: 100% 全部完成
**项目周期**: 第0周准备 → 批次泄漏复盘 → 二分类 → 可解释性 → 富集 → 技术报告 → 仓库收尾 全部完成

> ⚠️ **核心教训贯穿始终**：此前「基线 100% 准确率」是批次泄漏假象，已修正。后续一律用 LODO 留一数据集诚实评估，任何跨样本统计量只在训练集拟合。

## [GOAL] 项目目标
构建基于 Transformer 的基因表达癌症分类模型，支持 BRCA、LUAD、COAD 三种癌症类型预测，并提供可解释性分析。
—— 因「批次=类别」混杂，主任务从「三癌多分类」降级为「肿瘤 vs 正常」二分类（方案 B），以得到有意义的跨平台信号。

## [CRITICAL] 批次泄漏修正（2026-09-09）
- **错误现象**：测试准确率 100%、macro-F1 = 1.0000、CV std=0%。
- **根因**：批次=类别（1:1 混杂）+ 全局 z-score/HVG 泄漏 + 随机划分 + 量纲不一致。
- **修正**：探针→基因映射 + 三数据集交集(≈14,725 基因) + LODO 划分 + 训练集拟合统计量 + ComBat 仅诊断。
- **修正后诚实结论**：LODO macro-F1 = 0.0000（留出的全新癌种无法被认出）→ 证明原 100% 是泄漏。

## [COMPLETED] 已完成任务

### 1-5. 环境 / 数据 / 预处理 / 基线 / 批次诊断（100%）
- Python 3.11 + CPU torch；GSE45827/GSE31210/GSE39582 全部解析（986 样本）；基因级矩阵 986×14,725；RF/SVM 走 LODO；`results/lodo_results.yaml` + `batch_diagnosis.yaml` + 诚实报告。

### 6. 肿瘤 vs 正常二分类（方案 B，100%）⭐ 本轮完成
- **确认正常样本**：三数据集都有正常样本 —— BRCA 11 normal + 14 cell line、LUAD 20 normal、COAD 19 normal（合计 922 tumor / 50 normal / 14 exclude）。
- **落地**：`gene_mapping.extract_tumor_normal()` → `data/processed/tumor_normal.npy`；新增 `src/tumor_normal.py`（二分类 LODO + 随机对照）。

**二分类 LODO 诚实结果**（主指标 = AUC，阈值无关）：

| 指标 | RandomForest | SVM |
|---|---|---|
| **AUC** | 0.943 | **0.978** |
| oracle balAcc（test 最优阈值上限，泄漏） | 0.911 | 0.954 |
| 实际 balAcc（train 拟合阈值） | 0.500 | 0.500 |
| 随机划分对照 AUC / balAcc | 0.998 / 0.978 | 0.997 / 0.808 |

**结论**：
1. **肿瘤 vs 正常的生物学信号真实存在且能跨平台**（AUC 0.94–0.98，oracle balAcc 0.91–0.95），与三癌任务 LODO macroF1=0.0 形成鲜明对比 —— 这是方案 B 的核心价值。
2. **唯一遗留问题 = 阈值校准**：类别极不平衡(922:50) + 跨平台分数尺度偏移，训练集拟合的决策阈值无法迁移，硬标签退化为「全判 tumor」（normal recall=0）。batch-only ComBat 不解决（SVM 反而更差 AUC 0.883）。
3. 因此**主指标固定为 AUC**；硬标签需在训练集拟合阈值，跨平台校准是已知诚实局限。

## [COMPLETED] 7-9. Transformer 二分类 / 可解释性 / 富集 / 技术报告（100%）⭐ 2026-09-13
- **Transformer 二分类 LODO**：PR-AUC 0.9963 ± 0.0029 / ROC-AUC 0.9637 ± 0.0192，与 SVM 同档（SVM PR-AUC 0.9986 / AUC 0.9776 略高）。主指标最终定为 **PR-AUC / AUC**（阈值无关），弃用 balAcc/accuracy/F1。
- **硬标签分位数对齐**：测试分布按训练正常占比取 percentile，normal_recall 0 → 0.54/0.59；`class_weight(18)≈balanced` 不解决 domain shift（分数缩放偏移才是真因）。
- **可解释性（SHAP + Attention，纯推理、免训练）**：SHAP = Deep SHAP gradient×input；Attention = [CLS] 手算 CLS 行（与 torch 精确一致 1e-9）。跨折 rank-based 聚合（含缺席惩罚）。共识基因 = 肿瘤基质/ECM + 代谢（PFKFB3/CDO1/LCN2/COL11A1/DCN/DPT/CA4…）。
- **通路富集（Enrichr）**：唯一 FDR 显著 = ECM/胶原（GO:CC "Collagen-Containing ECM" adj p=3.9e-6）；KEGG/GO:BP/MF 不显著——共识是「肿瘤基质签名」而非癌基因通路。
- **技术报告**：`report/technical_report.md`（英文）+ `report/technical_report_zh.md`（中文）；README 已同步真实结果。
- **产物**：`src/explain_tn.py`、`src/enrichment.py`、`results/tn_interpretability_top_genes.csv`、`results/enrichment_results.csv`、`results/figures/*`（SHAP/attention/富集气泡图）。

## [COMPLETED] 仓库收尾（100%）⭐ 2026-09-13
1. **git 提交**：commit `88ca592` —— src/ 全部源码、results/（yaml/csv/txt/png）、report/（英/中 md + css + 两份 PDF）、tests/、notebooks/、README、config.yaml、.gitignore、requirements.txt 全部入库。
2. **根目录清理**：一次性脚本 / 散图 / `results_tn.zip` / `GeneTransformer.zip` / `GeneTransformer/` 已挪 `scratch/`（gitignored）。
3. **README 顶部一致性**：Intro / Highlights / Pipeline / Model arch / 目录结构 / Quick Start 全部改为二分类 + 批次泄漏 + 富集框架。
4. **.gitignore 补全**：`*.npz` / `*.zip` / `/*.png` / `/scratch/` / `/GeneTransformer/` / `results/legacy_random_split/` / `.pytest_cache/`。
5. **tests / notebooks / PDF**：15 个数据无关单测（`pytest` 全绿）；3 个 notebook（01 数据探索 / 02 基线 / 03 可解释性）；报告经 pandoc → HTML → headless Chrome 导两份 PDF（中文版已嵌 CJK 字体）。

## [INSIGHT] 关键教训
1. **「完美到不真实」的指标先怀疑泄漏**：100% / std=0% 是红灯不是绿灯。
2. **跨数据集任务必须用 LODO / grouped split**，不能用随机划分。
3. **任何跨样本统计量只在训练集拟合**（z-score / HVG / ComBat）。
4. **不平衡二分类以 AUC 为主指标**，硬标签受阈值/校准支配，别被 accuracy 误导。
5. **数据本身就是答案的一部分**：batch == class 时学到的是平台；换成肿瘤/正常后信号才真实浮现。

---
**项目状态**: ✅ 全部完成（批次泄漏复盘 → 二分类 → SHAP/Attention → ECM 富集 → 英/中技术报告 → 仓库收尾提交）
**剩余**: 无
